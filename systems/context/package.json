{
  "name": "@nexus/systems-context",
  "version": "1.0.0",
  "description": "Context engine for Nexus - cache, compression, prioritization, routing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nexus/core": "workspace:*",
    "@nexus/systems-memory": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "nexus",
    "context",
    "compression",
    "cache",
    "prioritization"
  ]
}